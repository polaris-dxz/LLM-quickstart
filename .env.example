# 复制本文件为项目根目录的 .env 并填写（.env 已被 git 忽略）
#
# OpenAI 官方：OPENAI_BASE_URL 可省略（默认 https://api.openai.com/v1）
# 第三方 OpenAI 兼容接口（如 api.apiyi.com）：填写其 base URL，末尾为 /v1（与 curl 一致）

OPENAI_API_KEY=sk-your-key-here
OPENAI_BASE_URL=https://api.apiyi.com/v1
